<x-app-layout>
    <x-slot name="header"><h2 class="text-xl font-semibold leading-tight text-slate-900">Detail Reward</h2></x-slot>
    <div class="py-8"><div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8">
            <div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h1 class="text-2xl font-bold text-slate-900">Detail Reward</h1><p class="mt-1 text-sm text-slate-500">Lihat detail reward dan ajukan penukaran jika poin mencukupi.</p></div><a href="{{ route('toko.rewards.index') }}" class="inline-flex items-center justify-center rounded-xl border border-slate-300 bg-white px-4 py-2.5 text-sm font-semibold text-slate-700 transition hover:bg-slate-50">Kembali</a></div>
            @if (session('error'))<div class="mb-4 rounded-2xl bg-red-50 p-4 text-sm font-medium text-red-700">{{ session('error') }}</div>@endif
            <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="grid grid-cols-1 gap-6 md:grid-cols-2"><div><p class="text-sm text-slate-500">Kode Reward</p><p class="mt-1 font-semibold text-slate-900">{{ $reward->reward_code }}</p></div><div><p class="text-sm text-slate-500">Nama Reward</p><p class="mt-1 font-semibold text-slate-900">{{ $reward->reward_name }}</p></div><div class="rounded-2xl bg-blue-50 p-4"><p class="text-sm text-blue-800">Poin Dibutuhkan</p><p class="mt-1 text-3xl font-bold text-blue-700">{{ number_format($reward->required_points) }}</p></div><div class="rounded-2xl bg-slate-50 p-4"><p class="text-sm text-slate-500">Poin Toko/Kios Saat Ini</p><p class="mt-1 text-3xl font-bold text-slate-900">{{ number_format(auth()->user()->store->total_points ?? 0) }}</p></div><div><p class="text-sm text-slate-500">Stok</p><p class="mt-1 font-semibold text-slate-900">{{ number_format($reward->stock) }}</p></div><div><p class="text-sm text-slate-500">Bisa Ditukar Oleh</p><p class="mt-1 font-semibold text-slate-900">{{ $reward->redeemable_by === 'both' ? 'Konsumen & Toko/Kios' : 'Toko/Kios' }}</p></div><div class="md:col-span-2"><p class="text-sm text-slate-500">Deskripsi</p><p class="mt-1 leading-6 text-slate-700">{{ $reward->description ?? '-' }}</p></div></div><div class="mt-6 flex justify-end"><form action="{{ route('toko.rewards.redeem', $reward) }}" method="POST" onsubmit="return confirm('Ajukan penukaran reward ini?')">@csrf<button type="submit" class="inline-flex items-center justify-center rounded-xl bg-green-600 px-5 py-2.5 text-sm font-semibold text-white shadow-sm transition hover:bg-green-700">Ajukan Penukaran</button></form></div></div>
        </div></div>
</x-app-layout>
