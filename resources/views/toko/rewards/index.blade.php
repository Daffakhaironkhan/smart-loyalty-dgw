<x-app-layout>
    <x-slot name="header"><h2 class="text-xl font-semibold leading-tight text-slate-900">Katalog Reward</h2></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h1 class="text-2xl font-bold text-slate-900">Katalog Reward</h1><p class="mt-1 text-sm text-slate-500">Pilih reward yang tersedia untuk ditukarkan menggunakan poin Toko/Kios.</p></div><div class="rounded-2xl bg-blue-50 px-5 py-3 text-sm font-semibold text-blue-700">Poin: {{ number_format(auth()->user()->store->total_points ?? 0) }}</div></div>
            <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">@forelse ($rewards as $reward)<div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p class="text-sm text-slate-500">{{ $reward->reward_code }}</p><h3 class="mt-2 text-lg font-bold text-slate-900">{{ $reward->reward_name }}</h3><p class="mt-2 line-clamp-3 text-sm leading-6 text-slate-500">{{ $reward->description ?? 'Tidak ada deskripsi.' }}</p><div class="mt-5 grid grid-cols-2 gap-3"><div class="rounded-xl bg-slate-50 p-3"><p class="text-xs text-slate-500">Poin</p><p class="mt-1 font-bold text-blue-700">{{ number_format($reward->required_points) }}</p></div><div class="rounded-xl bg-slate-50 p-3"><p class="text-xs text-slate-500">Stok</p><p class="mt-1 font-bold text-slate-900">{{ number_format($reward->stock) }}</p></div></div><a href="{{ route('toko.rewards.show', $reward) }}" class="mt-5 inline-flex w-full items-center justify-center rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-blue-700">Lihat Detail</a></div>@empty<div class="col-span-full rounded-2xl border border-slate-200 bg-white p-8 text-center text-slate-500">Belum ada reward yang tersedia untuk Toko/Kios.</div>@endforelse</div><div class="mt-4">{{ $rewards->links() }}</div>
        </div></div>
</x-app-layout>
