<x-app-layout>
    <x-slot name="header"><h2 class="text-xl font-semibold leading-tight text-slate-900">Riwayat Poin</h2></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="mb-6 rounded-3xl bg-gradient-to-br from-blue-950 via-slate-950 to-indigo-950 p-6 text-white shadow-sm"><p class="text-sm text-blue-100">Total Poin Toko/Kios</p><h1 class="mt-2 text-4xl font-bold">{{ number_format(auth()->user()->store->total_points ?? 0) }}</h1></div>
            <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-[850px] w-full text-left text-sm"><thead class="bg-slate-50 text-slate-700"><tr><th class="px-4 py-3">Tanggal</th><th class="px-4 py-3">Sumber</th><th class="px-4 py-3">Jenis</th><th class="px-4 py-3">Poin</th><th class="px-4 py-3">Catatan</th></tr></thead><tbody>@forelse ($histories as $history)<tr class="border-t border-slate-100"><td class="px-4 py-3 text-slate-700">{{ $history->created_at }}</td><td class="px-4 py-3 text-slate-700">@if ($history->source_type === 'store_purchase_transaction') Pembelian ke DGW @elseif ($history->source_type === 'reward_redemption') Penukaran Reward @else {{ ucwords(str_replace('_', ' ', $history->source_type)) }} @endif</td><td class="px-4 py-3">@if ($history->point_type === 'in')<span class="rounded-full bg-green-100 px-2.5 py-1 text-xs font-medium text-green-700">Masuk</span>@else<span class="rounded-full bg-red-100 px-2.5 py-1 text-xs font-medium text-red-700">Keluar</span>@endif</td><td class="px-4 py-3 font-semibold {{ $history->point_type === 'in' ? 'text-green-700' : 'text-red-700' }}">{{ $history->point_type === 'in' ? '+' : '-' }}{{ number_format($history->points) }}</td><td class="px-4 py-3 text-slate-700">{{ $history->description ?? '-' }}</td></tr>@empty<tr><td colspan="5" class="px-4 py-8 text-center text-slate-500">Belum ada riwayat poin.</td></tr>@endforelse</tbody></table></div></div><div class="mt-4">{{ $histories->links() }}</div>
        </div></div>
</x-app-layout>
